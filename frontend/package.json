{
  "name": "casino-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Phaser client for the casino. Builds into ../static, which the Rust server serves at /.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
